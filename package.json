{
  "name": "ngo-download-app-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd project && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}